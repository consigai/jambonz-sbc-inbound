name: AWS Service CI/CD

on:
  push:
    branches: [consig-*]

jobs:
  call-workflow-passing-data:
    uses: consigai/jambonz-sbc-inbound/.github/workflows/cdk-deploy.yml@consig-0.9.4-1-SBC
    permissions:
      actions: write
      contents: read
      id-token: write
    with:
      stack: jambonzSbcInbound
